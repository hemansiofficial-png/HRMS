'use client';import { useState, useEffect } from 'react';import { useSession } from 'next-auth/react';import { AppShell } from '@/components/layout/app-shell';import { Card } from '@/components/ui/card';import {  Calendar,  Plus,  Edit,  Trash2,  AlertCircle,  CheckCircle,  Clock,  FileText,} from 'lucide-react';interface LeavePolicy {  id: string;  leaveType: string;  allocatedDays: number;  carryForward: number;  requiresApproval: boolean;  description: string;  status: 'ACTIVE' | 'INACTIVE';}export default function LeavePolicyPage() {  const { data: session } = useSession();  const [policies, setPolicies] = useState<LeavePolicy[]>([]);  const [loading, setLoading] = useState(true);  const [showForm, setShowForm] = useState(false);  const [editingId, setEditingId] = useState<string | null>(null);  const [formData, setFormData] = useState({    leaveType: '',    allocatedDays: 0,    carryForward: 0,    requiresApproval: false,    description: '',  });  useEffect(() => {    fetchPolicies();  }, []);  const fetchPolicies = async () => {    try {      setLoading(true);      const response = await fetch('/api/leave/policies');      if (response.ok) {        const { data } = await response.json();        setPolicies(Array.isArray(data) ? data : []);      }    } catch (error) {      console.error('Failed to fetch policies:', error);      setPolicies([]);    } finally {      setLoading(false);    }  };  const handleInputChange = (e: React.ChangeEvent<HTMLInputElement | HTMLTextAreaElement>) => {    const { name, value, type } = e.target as any;    setFormData({      ...formData,      [name]: type === 'checkbox' ? (e.target as HTMLInputElement).checked : value,    });  };  const handleSubmit = async (e: React.FormEvent) => {    e.preventDefault();    try {      const method = editingId ? 'PUT' : 'POST';      const url = editingId ? `/api/leave/policies/${editingId}` : '/api/leave/policies';      const response = await fetch(url, {        method,        headers: { 'Content-Type': 'application/json' },        body: JSON.stringify(formData),      });      if (response.ok) {        fetchPolicies();        setShowForm(false);        setEditingId(null);        setFormData({          leaveType: '',          allocatedDays: 0,          carryForward: 0,          requiresApproval: false,          description: '',        });      }    } catch (error) {      console.error('Failed to save policy:', error);    }  };  const handleDelete = async (id: string) => {    if (confirm('Are you sure you want to delete this policy?')) {      try {        const response = await fetch(`/api/leave/policies/${id}`, { method: 'DELETE' });        if (response.ok) {          fetchPolicies();        }      } catch (error) {        console.error('Failed to delete policy:', error);      }    }  };  return (    <AppShell title="Leave Policies">      <div className="space-y-6">        {/* Header */}        <div className="flex justify-between items-center">          <div>            <h1 className="text-3xl font-bold" style={{ color: 'var(--text-primary)' }}>              Leave Policies            </h1>            <p style={{ color: 'var(--text-secondary)' }} className="mt-1">              Manage organization leave policies            </p>          </div>          <button            onClick={() => {              setShowForm(!showForm);              setEditingId(null);            }}            className="flex items-center gap-2 px-4 py-2 rounded-lg text-white font-medium hover:opacity-90"            style={{ backgroundColor: 'var(--brand)' }}          >            <Plus className="w-4 h-4" />            New Policy          </button>        </div>        {/* Form */}        {showForm && (          <Card className="p-6">            <form onSubmit={handleSubmit} className="space-y-4">              <div className="grid grid-cols-1 md:grid-cols-2 gap-4">                <div>                  <label className="block text-sm font-medium mb-1" style={{ color: 'var(--text-secondary)' }}>                    Leave Type                  </label>                  <input                    type="text"                    name="leaveType"                    value={formData.leaveType}                    onChange={handleInputChange}                    className="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2"                    style={{                      borderColor: 'var(--border)',                      backgroundColor: 'var(--card)',                      color: 'var(--text-primary)',                    }}                    required                  />                </div>                <div>                  <label className="block text-sm font-medium mb-1" style={{ color: 'var(--text-secondary)' }}>                    Allocated Days                  </label>                  <input                    type="number"                    name="allocatedDays"                    value={formData.allocatedDays}                    onChange={handleInputChange}                    className="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2"                    style={{                      borderColor: 'var(--border)',                      backgroundColor: 'var(--card)',                      color: 'var(--text-primary)',                    }}                    required                  />                </div>              </div>              <div className="grid grid-cols-1 md:grid-cols-2 gap-4">                <div>                  <label className="block text-sm font-medium mb-1" style={{ color: 'var(--text-secondary)' }}>                    Carry Forward Days                  </label>                  <input                    type="number"                    name="carryForward"                    value={formData.carryForward}                    onChange={handleInputChange}                    className="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2"                    style={{                      borderColor: 'var(--border)',                      backgroundColor: 'var(--card)',                      color: 'var(--text-primary)',                    }}                  />                </div>                <div className="flex items-end">                  <label className="flex items-center gap-2 cursor-pointer">                    <input                      type="checkbox"                      name="requiresApproval"                      checked={formData.requiresApproval}                      onChange={handleInputChange}                      className="w-4 h-4 rounded"                    />                    <span style={{ color: 'var(--text-secondary)' }} className="text-sm font-medium">                      Requires Approval                    </span>                  </label>                </div>              </div>              <div>                <label className="block text-sm font-medium mb-1" style={{ color: 'var(--text-secondary)' }}>                  Description                </label>                <textarea                  name="description"                  value={formData.description}                  onChange={handleInputChange}                  rows={3}                  className="w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring-2"                  style={{                    borderColor: 'var(--border)',                    backgroundColor: 'var(--card)',                    color: 'var(--text-primary)',                  }}                />              </div>              <div className="flex gap-2">                <button                  type="submit"                  className="px-4 py-2 rounded-lg text-white font-medium hover:opacity-90"                  style={{ backgroundColor: 'var(--success)' }}                >                  {editingId ? 'Update' : 'Create'} Policy                </button>                <button                  type="button"                  onClick={() => {                    setShowForm(false);                    setEditingId(null);                  }}                  className="px-4 py-2 border rounded-lg font-medium"                  style={{ borderColor: 'var(--border)', color: 'var(--text-secondary)' }}                >                  Cancel                </button>              </div>            </form>          </Card>        )}        {/* Stats */}        <div className="grid grid-cols-1 md:grid-cols-3 gap-4">          <Card className="p-6">            <div className="flex justify-between items-start">              <div>                <p style={{ color: 'var(--text-secondary)' }} className="text-sm">                  Total Policies                </p>                <p style={{ color: 'var(--brand)' }} className="text-3xl font-bold mt-2">                  {policies.length}                </p>              </div>              <FileText className="w-8 h-8" style={{ color: 'var(--brand)' }} />            </div>          </Card>          <Card className="p-6">            <div className="flex justify-between items-start">              <div>                <p style={{ color: 'var(--text-secondary)' }} className="text-sm">                  Active                </p>                <p style={{ color: 'var(--success)' }} className="text-3xl font-bold mt-2">                  {policies.filter((p) => p.status === 'ACTIVE').length}                </p>              </div>              <CheckCircle className="w-8 h-8" style={{ color: 'var(--success)' }} />            </div>          </Card>          <Card className="p-6">            <div className="flex justify-between items-start">              <div>                <p style={{ color: 'var(--text-secondary)' }} className="text-sm">                  Requires Approval                </p>                <p style={{ color: 'var(--warning)' }} className="text-3xl font-bold mt-2">                  {policies.filter((p) => p.requiresApproval).length}                </p>              </div>              <AlertCircle className="w-8 h-8" style={{ color: 'var(--warning)' }} />            </div>          </Card>        </div>        {/* Policies List */}        <Card>          {policies.length > 0 ? (            <div className="overflow-x-auto">              <table className="w-full">                <thead>                  <tr style={{ borderColor: 'var(--border)', borderBottom: '1px solid' }}>                    <th className="px-6 py-4 text-left text-sm font-semibold" style={{ color: 'var(--text-secondary)' }}>                      Leave Type                    </th>                    <th className="px-6 py-4 text-left text-sm font-semibold" style={{ color: 'var(--text-secondary)' }}>                      Days                    </th>                    <th className="px-6 py-4 text-left text-sm font-semibold" style={{ color: 'var(--text-secondary)' }}>                      Carry Forward                    </th>                    <th className="px-6 py-4 text-left text-sm font-semibold" style={{ color: 'var(--text-secondary)' }}>                      Approval                    </th>                    <th className="px-6 py-4 text-left text-sm font-semibold" style={{ color: 'var(--text-secondary)' }}>                      Actions                    </th>                  </tr>                </thead>                <tbody>                  {policies.map((policy, index) => (                    <tr                      key={policy?.id || `policy-${index}`}                      style={{                        borderColor: 'var(--border)',                        borderBottom: index < policies.length - 1 ? '1px solid' : 'none',                      }}                    >                      <td className="px-6 py-4">                        <p style={{ color: 'var(--text-primary)' }} className="font-medium">                          {policy.leaveType}                        </p>                      </td>                      <td className="px-6 py-4">                        <p style={{ color: 'var(--text-secondary)' }}>{policy.allocatedDays} days</p>                      </td>                      <td className="px-6 py-4">                        <p style={{ color: 'var(--text-secondary)' }}>{policy.carryForward} days</p>                      </td>                      <td className="px-6 py-4">                        <span                          className={`px-3 py-1 rounded-full text-xs font-semibold ${                            policy.requiresApproval ? 'bg-yellow-100 text-yellow-800' : 'bg-green-100 text-green-800'                          }`}                        >                          {policy.requiresApproval ? 'Yes' : 'No'}                        </span>                      </td>                      <td className="px-6 py-4">                        <div className="flex gap-2">                          <button className="p-1 hover:bg-gray-100 rounded" style={{ color: 'var(--brand)' }}>                            <Edit className="w-4 h-4" />                          </button>                          <button                            onClick={() => handleDelete(policy.id)}                            className="p-1 hover:bg-red-100 rounded"                            style={{ color: 'var(--danger)' }}                          >                            <Trash2 className="w-4 h-4" />                          </button>                        </div>                      </td>                    </tr>                  ))}                </tbody>              </table>            </div>          ) : (            <div className="text-center py-12">              <p style={{ color: 'var(--text-secondary)' }}>No policies configured yet</p>            </div>          )}        </Card>      </div>    </AppShell>  );}