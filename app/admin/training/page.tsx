'use client';import { useState } from 'react';import { AppShell } from '@/components/layout/app-shell';import { Card } from '@/components/ui/card';import { Plus, Search, CheckCircle, Clock, Award } from 'lucide-react';interface Training {  id: string;  title: string;  category: string;  duration: string;  level: 'beginner' | 'intermediate' | 'advanced';  status: 'not-started' | 'in-progress' | 'completed';  progress: number;  enrolledDate: string;  completionDate?: string;}export default function TrainingPage() {  const [trainings, setTrainings] = useState<Training[]>([    {      id: '1',      title: 'Advanced JavaScript',      category: 'Technical Skills',      duration: '20 hours',      level: 'advanced',      status: 'completed',      progress: 100,      enrolledDate: '2024-01-10',      completionDate: '2024-02-15',    },    {      id: '2',      title: 'Leadership Fundamentals',      category: 'Management',      duration: '15 hours',      level: 'intermediate',      status: 'in-progress',      progress: 65,      enrolledDate: '2024-02-01',    },    {      id: '3',      title: 'Customer Service Excellence',      category: 'Soft Skills',      duration: '8 hours',      level: 'beginner',      status: 'not-started',      progress: 0,      enrolledDate: '2024-02-20',    },  ]);  const [searchTerm, setSearchTerm] = useState('');  const [filterLevel, setFilterLevel] = useState<string>('all');  const [filterStatus, setFilterStatus] = useState<string>('all');  const [showForm, setShowForm] = useState(false);  const [formData, setFormData] = useState({    title: '',    category: '',    duration: '',    level: 'beginner' as const,  });  const handleChange = (e: React.ChangeEvent<HTMLInputElement | HTMLSelectElement>) => {    const { name, value } = e.target;    setFormData(prev => ({ ...prev, [name]: value }));  };  const handleSubmit = (e: React.FormEvent) => {    e.preventDefault();    const newTraining: Training = {      id: String(trainings.length + 1),      title: formData.title,      category: formData.category,      duration: formData.duration,      level: formData.level,      status: 'not-started',      progress: 0,      enrolledDate: new Date().toISOString().split('T')[0],    };    setTrainings([...trainings, newTraining]);    setFormData({ title: '', category: '', duration: '', level: 'beginner' });    setShowForm(false);  };  const updateProgress = (id: string, progress: number) => {    setTrainings(trainings.map(t => ({      ...t,      id: t.id,      progress: Math.min(100, Math.max(0, progress)),      status: progress === 100 ? 'completed' : progress > 0 ? 'in-progress' : 'not-started',      completionDate: progress === 100 ? new Date().toISOString().split('T')[0] : t.completionDate,    } as Training)));  };  const deleteTraining = (id: string) => {    if (confirm('Remove this training?')) {      setTrainings(trainings.filter(t => t.id !== id));    }  };  let filtered = trainings.filter(t =>    t.title.toLowerCase().includes(searchTerm.toLowerCase())  );  if (filterLevel !== 'all') {    filtered = filtered.filter(t => t.level === filterLevel);  }  if (filterStatus !== 'all') {    filtered = filtered.filter(t => t.status === filterStatus);  }  const stats = {    total: trainings.length,    completed: trainings.filter(t => t.status === 'completed').length,    inProgress: trainings.filter(t => t.status === 'in-progress').length,    notStarted: trainings.filter(t => t.status === 'not-started').length,  };  const getStatusColor = (status: Training['status']) => {    switch (status) {      case 'completed':        return 'bg-green-100 text-green-800';      case 'in-progress':        return 'bg-blue-100 text-blue-800';      default:        return 'bg-gray-100 text-gray-800';    }  };  return (    <AppShell title="Training & Learning">      <div className="space-y-6">        {/* Stats */}        <div className="grid gap-4 md:grid-cols-4">          <Card className="p-6">            <div>              <p className="text-sm text-gray-600">Total Courses</p>              <p className="text-3xl font-bold text-blue-600 mt-2">{stats.total}</p>            </div>          </Card>          <Card className="p-6">            <div>              <p className="text-sm text-gray-600">Completed</p>              <p className="text-3xl font-bold text-green-600 mt-2">{stats.completed}</p>            </div>          </Card>          <Card className="p-6">            <div>              <p className="text-sm text-gray-600">In Progress</p>              <p className="text-3xl font-bold text-blue-600 mt-2">{stats.inProgress}</p>            </div>          </Card>          <Card className="p-6">            <div>              <p className="text-sm text-gray-600">Not Started</p>              <p className="text-3xl font-bold text-yellow-600 mt-2">{stats.notStarted}</p>            </div>          </Card>        </div>        {/* Header */}        <div className="flex justify-between items-center">          <h2 className="text-2xl font-bold">My Learning Path</h2>          <button            onClick={() => setShowForm(!showForm)}            className="bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg transition flex items-center gap-2"          >            <Plus className="w-4 h-4" /> Enroll Course          </button>        </div>        {/* Filters */}        <div className="flex gap-4 flex-wrap">          <div className="relative flex-1 min-w-[250px]">            <Search className="absolute left-3 top-3 text-gray-400 w-5 h-5" />            <input              type="text"              placeholder="Search courses..."              value={searchTerm}              onChange={(e) => setSearchTerm(e.target.value)}              className="w-full pl-10 pr-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500"            />          </div>          <select            value={filterLevel}            onChange={(e) => setFilterLevel(e.target.value)}            className="px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500"          >            <option value="all">All Levels</option>            <option value="beginner">Beginner</option>            <option value="intermediate">Intermediate</option>            <option value="advanced">Advanced</option>          </select>          <select            value={filterStatus}            onChange={(e) => setFilterStatus(e.target.value)}            className="px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500"          >            <option value="all">All Status</option>            <option value="completed">Completed</option>            <option value="in-progress">In Progress</option>            <option value="not-started">Not Started</option>          </select>        </div>        {/* Form */}        {showForm && (          <Card className="p-6">            <h3 className="text-lg font-semibold mb-4">Enroll in a Course</h3>            <form onSubmit={handleSubmit} className="space-y-4">              <div>                <label className="block text-sm font-medium text-gray-700 mb-2">Course Title *</label>                <input                  type="text"                  name="title"                  value={formData.title}                  onChange={handleChange}                  required                  className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500"                  placeholder="e.g., Python for Data Science"                />              </div>              <div className="grid grid-cols-3 gap-4">                <div>                  <label className="block text-sm font-medium text-gray-700 mb-2">Category *</label>                  <input                    type="text"                    name="category"                    value={formData.category}                    onChange={handleChange}                    required                    className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500"                    placeholder="e.g., Technical Skills"                  />                </div>                <div>                  <label className="block text-sm font-medium text-gray-700 mb-2">Duration *</label>                  <input                    type="text"                    name="duration"                    value={formData.duration}                    onChange={handleChange}                    required                    className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500"                    placeholder="e.g., 20 hours"                  />                </div>                <div>                  <label className="block text-sm font-medium text-gray-700 mb-2">Level *</label>                  <select                    name="level"                    value={formData.level}                    onChange={handleChange}                    className="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500"                  >                    <option value="beginner">Beginner</option>                    <option value="intermediate">Intermediate</option>                    <option value="advanced">Advanced</option>                  </select>                </div>              </div>              <div className="flex gap-2">                <button                  type="submit"                  className="flex-1 bg-blue-600 hover:bg-blue-700 text-white font-medium py-2 rounded-lg transition"                >                  Enroll Course                </button>                <button                  type="button"                  onClick={() => setShowForm(false)}                  className="flex-1 bg-gray-200 hover:bg-gray-300 text-gray-900 font-medium py-2 rounded-lg transition"                >                  Cancel                </button>              </div>            </form>          </Card>        )}        {/* Trainings Grid */}        <div className="grid gap-4">          {filtered.map(training => (            <Card key={training.id} className="p-6">              <div className="flex items-start justify-between mb-4">                <div className="flex-1">                  <div className="flex items-center gap-3 mb-2">                    <h3 className="text-lg font-semibold">{training.title}</h3>                    <span className={`px-2 py-1 rounded text-xs font-medium ${getStatusColor(training.status)}`}>                      {training.status === 'not-started' ? 'Not Started' :                       training.status === 'in-progress' ? 'In Progress' : 'Completed'}                    </span>                    <span className="px-2 py-1 rounded text-xs font-medium bg-purple-100 text-purple-800">                      {training.level.charAt(0).toUpperCase() + training.level.slice(1)}                    </span>                  </div>                  <p className="text-sm text-gray-600 mb-1">{training.category}</p>                  <p className="text-xs text-gray-500">Duration: {training.duration}</p>                </div>                <div className="ml-4">                  {training.status === 'completed' && (                    <CheckCircle className="w-6 h-6 text-green-600" />                  )}                  {training.status === 'in-progress' && (                    <Clock className="w-6 h-6 text-blue-600" />                  )}                  {training.status === 'not-started' && (                    <Award className="w-6 h-6 text-gray-400" />                  )}                </div>              </div>              {/* Progress Bar */}              <div className="mb-4">                <div className="flex justify-between items-center mb-1">                  <span className="text-xs font-medium text-gray-600">Progress</span>                  <span className="text-xs font-bold text-gray-700">{training.progress}%</span>                </div>                <div className="w-full bg-gray-200 rounded-full h-2">                  <div                    className="bg-blue-600 h-2 rounded-full transition-all"                    style={{ width: `${training.progress}%` }}                  />                </div>              </div>              {/* Progress Controls */}              <div className="flex gap-2">                <button                  onClick={() => updateProgress(training.id, training.progress - 10)}                  disabled={training.progress === 0}                  className="flex-1 px-3 py-1 bg-gray-100 hover:bg-gray-200 text-gray-700 rounded text-sm font-medium transition disabled:opacity-50"                >                  - 10%                </button>                <button                  onClick={() => updateProgress(training.id, training.progress + 10)}                  disabled={training.progress === 100}                  className="flex-1 px-3 py-1 bg-blue-100 hover:bg-blue-200 text-blue-700 rounded text-sm font-medium transition disabled:opacity-50"                >                  + 10%                </button>                <button                  onClick={() => updateProgress(training.id, 100)}                  disabled={training.progress === 100}                  className="flex-1 px-3 py-1 bg-green-100 hover:bg-green-200 text-green-700 rounded text-sm font-medium transition disabled:opacity-50"                >                  Complete                </button>                <button                  onClick={() => deleteTraining(training.id)}                  className="flex-1 px-3 py-1 bg-red-100 hover:bg-red-200 text-red-700 rounded text-sm font-medium transition"                >                  Remove                </button>              </div>              {training.completionDate && (                <p className="text-xs text-gray-500 mt-3">                  Completed on {training.completionDate}                </p>              )}            </Card>          ))}        </div>        {filtered.length === 0 && (          <Card className="p-12 text-center">            <p className="text-gray-500">No training courses found.</p>          </Card>        )}      </div>    </AppShell>  );}