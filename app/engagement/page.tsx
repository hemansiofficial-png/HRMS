'use client';import { useState, useEffect } from 'react';import { useSession } from 'next-auth/react';import { AppShell } from '@/components/layout/app-shell';import { Card } from '@/components/ui/card';import {  BarChart3,  MessageCircle,  PieChart,  TrendingUp,  CheckCircle,  Clock,  LoaderCircle,} from 'lucide-react';interface Survey {  id: string;  title: string;  description: string;  type: string;  responseRate: number;  status: string;  createdDate: string;  questions: number;}export default function EngagementPage() {  const { data: session } = useSession();  const [surveys, setSurveys] = useState<Survey[]>([]);  const [loading, setLoading] = useState(true);  const [activeTab, setActiveTab] = useState<'available' | 'completed'>('available');  useEffect(() => {    fetchSurveys();  }, []);  const fetchSurveys = async () => {    try {      setLoading(true);      const response = await fetch('/api/engagement/surveys');      if (response.ok) {        const data = await response.json();        setSurveys(data);      }    } catch (error) {      console.error('Failed to fetch surveys:', error);    } finally {      setLoading(false);    }  };  const handleStartSurvey = async (surveyId: string) => {    window.location.href = `/engagement/survey/${surveyId}`;  };  if (loading) {    return (      <AppShell title="Employee Engagement">        <div className="flex items-center justify-center h-screen">          <LoaderCircle className="w-8 h-8 animate-spin text-blue-600" />        </div>      </AppShell>    );  }  const availableSurveys = surveys.filter((s) => s.status === 'ACTIVE');  const completedSurveys = surveys.filter((s) => s.status === 'COMPLETED');  return (    <AppShell title="Employee Engagement">      <div className="space-y-6">        {/* Header */}        <div>          <h1 className="text-3xl font-bold text-gray-900">Employee Engagement</h1>          <p className="text-gray-600 mt-1">            Participate in surveys to help improve workplace experience          </p>        </div>        {/* Stats */}        <div className="grid grid-cols-1 md:grid-cols-3 gap-4">          <Card className="p-6">            <div className="flex justify-between items-start mb-4">              <div>                <p className="text-gray-600 text-sm">Total Surveys</p>                <p className="text-3xl font-bold text-blue-600 mt-2">{surveys.length}</p>              </div>              <BarChart3 className="w-8 h-8 text-blue-600" />            </div>          </Card>          <Card className="p-6">            <div className="flex justify-between items-start mb-4">              <div>                <p className="text-gray-600 text-sm">Available</p>                <p className="text-3xl font-bold text-yellow-600 mt-2">{availableSurveys.length}</p>              </div>              <Clock className="w-8 h-8 text-yellow-600" />            </div>          </Card>          <Card className="p-6">            <div className="flex justify-between items-start mb-4">              <div>                <p className="text-gray-600 text-sm">Completed</p>                <p className="text-3xl font-bold text-green-600 mt-2">{completedSurveys.length}</p>              </div>              <CheckCircle className="w-8 h-8 text-green-600" />            </div>          </Card>        </div>        {/* Tabs */}        <div className="flex gap-4 border-b border-gray-200">          <button            onClick={() => setActiveTab('available')}            className={`px-4 py-2 font-medium flex items-center gap-2 ${              activeTab === 'available'                ? 'text-blue-600 border-b-2 border-blue-600'                : 'text-gray-600 hover:text-gray-900'            }`}          >            <MessageCircle className="w-4 h-4" />            Available Surveys          </button>          <button            onClick={() => setActiveTab('completed')}            className={`px-4 py-2 font-medium flex items-center gap-2 ${              activeTab === 'completed'                ? 'text-blue-600 border-b-2 border-blue-600'                : 'text-gray-600 hover:text-gray-900'            }`}          >            <CheckCircle className="w-4 h-4" />            Completed          </button>        </div>        {/* Surveys List */}        <div className="space-y-4">          {activeTab === 'available' && (            <>              {availableSurveys.length > 0 ? (                availableSurveys.map((survey) => (                  <Card key={survey.id} className="p-6 hover:shadow-lg transition">                    <div className="flex items-start justify-between gap-4">                      <div className="flex-1">                        <h3 className="font-semibold text-gray-900 text-lg mb-1">{survey.title}</h3>                        <p className="text-gray-600 mb-3">{survey.description}</p>                        <div className="flex items-center gap-4 text-sm text-gray-600">                          <div className="flex items-center gap-1">                            <BarChart3 className="w-4 h-4" />                            <span>{survey.questions} questions</span>                          </div>                          <div className="flex items-center gap-1">                            <TrendingUp className="w-4 h-4" />                            <span>{survey.responseRate}% responses</span>                          </div>                          <span className="text-xs text-gray-500">                            Created: {survey.createdDate}                          </span>                        </div>                      </div>                      <button                        onClick={() => handleStartSurvey(survey.id)}                        className="bg-blue-600 text-white px-6 py-2 rounded-lg hover:bg-blue-700 transition font-medium whitespace-nowrap"                      >                        Start                      </button>                    </div>                  </Card>                ))              ) : (                <Card className="p-12 text-center">                  <MessageCircle className="w-12 h-12 text-gray-400 mx-auto mb-4" />                  <p className="text-gray-600">No surveys available</p>                </Card>              )}            </>          )}          {activeTab === 'completed' && (            <>              {completedSurveys.length > 0 ? (                completedSurveys.map((survey) => (                  <Card key={survey.id} className="p-6 hover:shadow-lg transition">                    <div className="flex items-start justify-between gap-4">                      <div className="flex-1">                        <h3 className="font-semibold text-gray-900 text-lg mb-1 flex items-center gap-2">                          {survey.title}                          <CheckCircle className="w-5 h-5 text-green-600" />                        </h3>                        <p className="text-gray-600 mb-3">{survey.description}</p>                        <div className="flex items-center gap-4 text-sm text-gray-600">                          <div className="flex items-center gap-1">                            <BarChart3 className="w-4 h-4" />                            <span>{survey.questions} questions</span>                          </div>                          <span className="text-xs text-gray-500">                            Completed on {survey.createdDate}                          </span>                        </div>                      </div>                      <a                        href={`/engagement/survey/${survey.id}/results`}                        className="bg-gray-100 text-gray-700 px-6 py-2 rounded-lg hover:bg-gray-200 transition font-medium whitespace-nowrap"                      >                        View Results                      </a>                    </div>                  </Card>                ))              ) : (                <Card className="p-12 text-center">                  <PieChart className="w-12 h-12 text-gray-400 mx-auto mb-4" />                  <p className="text-gray-600">No completed surveys</p>                </Card>              )}            </>          )}        </div>      </div>    </AppShell>  );}