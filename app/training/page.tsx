'use client';import { useState, useEffect } from 'react';import { useSession } from 'next-auth/react';import { AppShell } from '@/components/layout/app-shell';import { Card } from '@/components/ui/card';import {  GraduationCap,  BookOpen,  Plus,  CheckCircle,  Clock,  Star,  Award,  LoaderCircle,} from 'lucide-react';interface Training {  id: string;  courseName: string;  provider: string;  enrollmentDate: string;  completionDate?: string;  status: string;  hours: number;  certificate: boolean;}export default function TrainingPage() {  const { data: session } = useSession();  const [trainings, setTrainings] = useState<Training[]>([]);  const [loading, setLoading] = useState(true);  const [availableCourses, setAvailableCourses] = useState<any[]>([]);  const [activeTab, setActiveTab] = useState<'enrolled' | 'available'>('enrolled');  useEffect(() => {    fetchTrainingData();  }, []);  const fetchTrainingData = async () => {    try {      setLoading(true);      const [enrolledRes, coursesRes] = await Promise.all([        fetch('/api/training/enrolled'),        fetch('/api/training/available-courses'),      ]);      if (enrolledRes.ok) {        const data = await enrolledRes.json();        setTrainings(data);      }      if (coursesRes.ok) {        const data = await coursesRes.json();        setAvailableCourses(data);      }    } catch (error) {      console.error('Failed to fetch training data:', error);    } finally {      setLoading(false);    }  };  const handleEnroll = async (courseId: string) => {    try {      const response = await fetch('/api/training/enroll', {        method: 'POST',        headers: { 'Content-Type': 'application/json' },        body: JSON.stringify({ courseId }),      });      if (response.ok) {        fetchTrainingData();      }    } catch (error) {      console.error('Enrollment failed:', error);    }  };  if (loading) {    return (      <AppShell title="Training & Learning">        <div className="flex items-center justify-center h-screen">          <LoaderCircle className="w-8 h-8 animate-spin text-blue-600" />        </div>      </AppShell>    );  }  const completed = trainings.filter((t) => t.status === 'COMPLETED').length;  const inProgress = trainings.filter((t) => t.status === 'IN_PROGRESS').length;  return (    <AppShell title="Training & Learning">      <div className="space-y-6">        {/* Header */}        <div>          <h1 className="text-3xl font-bold text-gray-900">Training & Learning</h1>          <p className="text-gray-600 mt-1">Enroll in courses and track your learning progress</p>        </div>        {/* Stats */}        <div className="grid grid-cols-1 md:grid-cols-4 gap-4">          <Card className="p-6">            <div className="flex justify-between items-start mb-4">              <div>                <p className="text-gray-600 text-sm">Total Enrollments</p>                <p className="text-3xl font-bold text-blue-600 mt-2">{trainings.length}</p>              </div>              <GraduationCap className="w-8 h-8 text-blue-600" />            </div>          </Card>          <Card className="p-6">            <div className="flex justify-between items-start mb-4">              <div>                <p className="text-gray-600 text-sm">In Progress</p>                <p className="text-3xl font-bold text-yellow-600 mt-2">{inProgress}</p>              </div>              <Clock className="w-8 h-8 text-yellow-600" />            </div>          </Card>          <Card className="p-6">            <div className="flex justify-between items-start mb-4">              <div>                <p className="text-gray-600 text-sm">Completed</p>                <p className="text-3xl font-bold text-green-600 mt-2">{completed}</p>              </div>              <CheckCircle className="w-8 h-8 text-green-600" />            </div>          </Card>          <Card className="p-6">            <div className="flex justify-between items-start mb-4">              <div>                <p className="text-gray-600 text-sm">Certificates</p>                <p className="text-3xl font-bold text-purple-600 mt-2">                  {trainings.filter((t) => t.certificate).length}                </p>              </div>              <Award className="w-8 h-8 text-purple-600" />            </div>          </Card>        </div>        {/* Tabs */}        <div className="flex gap-4 border-b border-gray-200">          <button            onClick={() => setActiveTab('enrolled')}            className={`px-4 py-2 font-medium flex items-center gap-2 ${              activeTab === 'enrolled'                ? 'text-blue-600 border-b-2 border-blue-600'                : 'text-gray-600 hover:text-gray-900'            }`}          >            <BookOpen className="w-4 h-4" />            My Courses          </button>          <button            onClick={() => setActiveTab('available')}            className={`px-4 py-2 font-medium flex items-center gap-2 ${              activeTab === 'available'                ? 'text-blue-600 border-b-2 border-blue-600'                : 'text-gray-600 hover:text-gray-900'            }`}          >            <Plus className="w-4 h-4" />            Available Courses          </button>        </div>        {/* Content */}        {activeTab === 'enrolled' && (          <div className="grid grid-cols-1 md:grid-cols-2 gap-4">            {trainings.length > 0 ? (              trainings.map((training) => (                <Card key={training.id} className="p-6 hover:shadow-lg transition">                  <div className="flex items-start justify-between mb-3">                    <div>                      <h3 className="font-semibold text-gray-900">{training.courseName}</h3>                      <p className="text-sm text-gray-600">{training.provider}</p>                    </div>                    {training.certificate && (                      <Award className="w-5 h-5 text-yellow-500" />                    )}                  </div>                  {training.status === 'IN_PROGRESS' && (                    <div className="mb-3">                      <div className="flex justify-between text-xs text-gray-600 mb-1">                        <span>Progress</span>                        <span>65%</span>                      </div>                      <div className="w-full bg-gray-200 rounded-full h-2">                        <div className="bg-blue-600 h-2 rounded-full w-2/3"></div>                      </div>                    </div>                  )}                  <div className="space-y-2 text-sm mb-4">                    <div className="flex justify-between text-gray-600">                      <span>Enrolled:</span>                      <span>{training.enrollmentDate}</span>                    </div>                    <div className="flex justify-between text-gray-600">                      <span>Duration:</span>                      <span>{training.hours} hours</span>                    </div>                    <div className="flex justify-between text-gray-600">                      <span>Status:</span>                      <span                        className={`px-2 py-1 rounded text-xs font-medium ${                          training.status === 'COMPLETED'                            ? 'bg-green-100 text-green-800'                            : 'bg-yellow-100 text-yellow-800'                        }`}                      >                        {training.status}                      </span>                    </div>                  </div>                  {training.completionDate && (                    <p className="text-xs text-green-600">                      Completed on {training.completionDate}                    </p>                  )}                </Card>              ))            ) : (              <Card className="col-span-full p-12 text-center">                <BookOpen className="w-12 h-12 text-gray-400 mx-auto mb-4" />                <p className="text-gray-600">No courses enrolled yet</p>              </Card>            )}          </div>        )}        {activeTab === 'available' && (          <div className="grid grid-cols-1 md:grid-cols-2 gap-4">            {availableCourses.length > 0 ? (              availableCourses.map((course) => (                <Card key={course.id} className="p-6 hover:shadow-lg transition">                  <div className="mb-3">                    <h3 className="font-semibold text-gray-900">{course.name}</h3>                    <p className="text-sm text-gray-600 mt-1">{course.description}</p>                  </div>                  <div className="space-y-2 text-sm mb-4">                    <div className="flex justify-between text-gray-600">                      <span>Provider:</span>                      <span className="font-medium">{course.provider}</span>                    </div>                    <div className="flex justify-between text-gray-600">                      <span>Duration:</span>                      <span className="font-medium">{course.hours} hours</span>                    </div>                    <div className="flex justify-between text-gray-600">                      <span>Difficulty:</span>                      <span className="font-medium">{course.difficulty}</span>                    </div>                  </div>                  <button                    onClick={() => handleEnroll(course.id)}                    className="w-full bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700 transition text-sm font-medium"                  >                    Enroll Now                  </button>                </Card>              ))            ) : (              <Card className="col-span-full p-12 text-center">                <GraduationCap className="w-12 h-12 text-gray-400 mx-auto mb-4" />                <p className="text-gray-600">No courses available</p>              </Card>            )}          </div>        )}      </div>    </AppShell>  );}