'use client';import { useState, useEffect } from 'react';import { useSession } from 'next-auth/react';import { AppShell } from '@/components/layout/app-shell';import { Card } from '@/components/ui/card';import {  Users,  MessageSquare,  CheckCircle,  Clock,  X,  Search,  Filter,  Mail,  Phone,  MapPin,  FileText,} from 'lucide-react';interface Candidate {  id: string;  name: string;  email: string;  phone: string;  position: string;  location: string;  status: 'APPLIED' | 'SCREENING' | 'INTERVIEW' | 'OFFER' | 'REJECTED' | 'HIRED';  appliedDate: string;  resume?: string;  notes?: string;}export default function CandidatesPage() {  const { data: session } = useSession();  const [candidates, setCandidates] = useState<Candidate[]>([]);  const [loading, setLoading] = useState(true);  const [searchTerm, setSearchTerm] = useState('');  const [filterStatus, setFilterStatus] = useState('all');  useEffect(() => {    fetchCandidates();  }, []);  const fetchCandidates = async () => {    try {      setLoading(true);      const response = await fetch('/api/recruitment/candidates');      if (response.ok) {        const { data } = await response.json();        setCandidates(Array.isArray(data) ? data : []);      }    } catch (error) {      console.error('Failed to fetch candidates:', error);      setCandidates([]);    } finally {      setLoading(false);    }  };  const getStatusColor = (status: string) => {    switch (status) {      case 'APPLIED':        return 'bg-blue-100 text-blue-800';      case 'SCREENING':        return 'bg-yellow-100 text-yellow-800';      case 'INTERVIEW':        return 'bg-purple-100 text-purple-800';      case 'OFFER':        return 'bg-green-100 text-green-800';      case 'HIRED':        return 'bg-green-600 text-white';      case 'REJECTED':        return 'bg-red-100 text-red-800';      default:        return 'bg-gray-100 text-gray-800';    }  };  const getStatusIcon = (status: string) => {    switch (status) {      case 'INTERVIEW':        return <MessageSquare className="w-4 h-4" />;      case 'OFFER':        return <CheckCircle className="w-4 h-4" />;      case 'HIRED':        return <CheckCircle className="w-4 h-4" />;      case 'REJECTED':        return <X className="w-4 h-4" />;      default:        return <Clock className="w-4 h-4" />;    }  };  const filteredCandidates = candidates.filter((c) =>    ((c?.name?.toLowerCase().includes(searchTerm.toLowerCase())) ||      (c?.email?.toLowerCase().includes(searchTerm.toLowerCase())) ||      (c?.position?.toLowerCase().includes(searchTerm.toLowerCase()))) &&    (filterStatus === 'all' || c?.status === filterStatus)  );  const stats = {    total: candidates.length,    applied: candidates.filter((c) => c.status === 'APPLIED').length,    interview: candidates.filter((c) => c.status === 'INTERVIEW').length,    hired: candidates.filter((c) => c.status === 'HIRED').length,  };  return (    <AppShell title="Candidates">      <div className="space-y-6">        {/* Header */}        <div className="flex justify-between items-center">          <div>            <h1 className="text-3xl font-bold" style={{ color: 'var(--text-primary)' }}>              Candidates Pipeline            </h1>            <p style={{ color: 'var(--text-secondary)' }} className="mt-1">              Manage and track job applicants            </p>          </div>        </div>        {/* Stats */}        <div className="grid grid-cols-1 md:grid-cols-4 gap-4">          <Card className="p-6">            <div className="flex justify-between items-start">              <div>                <p style={{ color: 'var(--text-secondary)' }} className="text-sm">                  Total Applicants                </p>                <p style={{ color: 'var(--brand)' }} className="text-3xl font-bold mt-2">                  {stats.total}                </p>              </div>              <Users className="w-8 h-8" style={{ color: 'var(--brand)' }} />            </div>          </Card>          <Card className="p-6">            <div className="flex justify-between items-start">              <div>                <p style={{ color: 'var(--text-secondary)' }} className="text-sm">                  New Applications                </p>                <p className="text-3xl font-bold mt-2" style={{ color: 'var(--info)' }}>                  {stats.applied}                </p>              </div>              <FileText className="w-8 h-8" style={{ color: 'var(--info)' }} />            </div>          </Card>          <Card className="p-6">            <div className="flex justify-between items-start">              <div>                <p style={{ color: 'var(--text-secondary)' }} className="text-sm">                  In Interview                </p>                <p className="text-3xl font-bold mt-2" style={{ color: 'var(--warning)' }}>                  {stats.interview}                </p>              </div>              <MessageSquare className="w-8 h-8" style={{ color: 'var(--warning)' }} />            </div>          </Card>          <Card className="p-6">            <div className="flex justify-between items-start">              <div>                <p style={{ color: 'var(--text-secondary)' }} className="text-sm">                  Hired                </p>                <p style={{ color: 'var(--success)' }} className="text-3xl font-bold mt-2">                  {stats.hired}                </p>              </div>              <CheckCircle className="w-8 h-8" style={{ color: 'var(--success)' }} />            </div>          </Card>        </div>        {/* Search & Filter */}        <div className="flex gap-4">          <div className="flex-1 relative">            <Search              className="absolute left-3 top-1/2 transform -translate-y-1/2 w-5 h-5"              style={{ color: 'var(--text-tertiary)' }}            />            <input              type="text"              placeholder="Search by name, email, or position..."              value={searchTerm}              onChange={(e) => setSearchTerm(e.target.value)}              className="w-full pl-10 pr-4 py-2 border rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500"              style={{                borderColor: 'var(--border)',                backgroundColor: 'var(--card)',                color: 'var(--text-primary)',              }}            />          </div>          <button            className="px-4 py-2 border rounded-lg flex items-center gap-2 hover:bg-gray-50"            style={{ borderColor: 'var(--border)' }}          >            <Filter className="w-4 h-4" />            Filter          </button>        </div>        {/* Candidates Table */}        <Card>          {filteredCandidates.length > 0 ? (            <div className="overflow-x-auto">              <table className="w-full">                <thead>                  <tr style={{ borderColor: 'var(--border)', borderBottom: '1px solid' }}>                    <th className="px-6 py-4 text-left text-sm font-semibold" style={{ color: 'var(--text-secondary)' }}>                      Name                    </th>                    <th className="px-6 py-4 text-left text-sm font-semibold" style={{ color: 'var(--text-secondary)' }}>                      Position                    </th>                    <th className="px-6 py-4 text-left text-sm font-semibold" style={{ color: 'var(--text-secondary)' }}>                      Contact                    </th>                    <th className="px-6 py-4 text-left text-sm font-semibold" style={{ color: 'var(--text-secondary)' }}>                      Status                    </th>                    <th className="px-6 py-4 text-left text-sm font-semibold" style={{ color: 'var(--text-secondary)' }}>                      Applied Date                    </th>                  </tr>                </thead>                <tbody>                  {filteredCandidates.map((candidate, index) => (                    <tr                      key={candidate?.id || `candidate-${index}`}                      style={{                        borderColor: 'var(--border)',                        borderBottom: index < filteredCandidates.length - 1 ? '1px solid' : 'none',                      }}                    >                      <td className="px-6 py-4">                        <p style={{ color: 'var(--text-primary)' }} className="font-medium">                          {candidate.name}                        </p>                      </td>                      <td className="px-6 py-4">                        <p style={{ color: 'var(--text-secondary)' }}>{candidate.position}</p>                      </td>                      <td className="px-6 py-4">                        <div className="space-y-1">                          <div className="flex items-center gap-2" style={{ color: 'var(--text-secondary)' }}>                            <Mail className="w-4 h-4" />                            <span className="text-sm">{candidate.email}</span>                          </div>                          <div className="flex items-center gap-2" style={{ color: 'var(--text-tertiary)' }}>                            <Phone className="w-4 h-4" />                            <span className="text-sm">{candidate.phone}</span>                          </div>                        </div>                      </td>                      <td className="px-6 py-4">                        <div className={`inline-flex items-center gap-1 px-3 py-1 rounded-full text-xs font-semibold ${getStatusColor(candidate.status)}`}>                          {getStatusIcon(candidate.status)}                          {candidate.status}                        </div>                      </td>                      <td className="px-6 py-4">                        <p style={{ color: 'var(--text-secondary)' }} className="text-sm">                          {new Date(candidate.appliedDate).toLocaleDateString()}                        </p>                      </td>                    </tr>                  ))}                </tbody>              </table>            </div>          ) : (            <div className="text-center py-12">              <p style={{ color: 'var(--text-secondary)' }}>No candidates found</p>            </div>          )}        </Card>      </div>    </AppShell>  );}